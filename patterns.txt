1.) Factory - 
	we used the design abstract factory in the models. We created an interface called Model.java and UMLModel.java implements it.

2.) Builder - 
	we used the builder design model in the models directory. Our models build up on each other, but the base objects we used, such as Method and
	Field, extends the object Formal. 
	
3.) MVC
	we used the MVC design model. Our models are in the models directory, the view files are in the views directory, and the controller
	files are in the controller directory. 
	
4.) 